# Claude Code 逆向工程 Bash 白名单方法

## 步骤

1. 输入 `/permissions`
2. 选 `1. Add a new rule...`
3. 输入 `Bash` 回车
4. 选 `1. Project settings (local)` 回车
5. 确认列表中出现 `Bash` 条目

完成。之后所有 Bash 操作不再被拦截。

（按方向键 ↓ 选到 bash 那一行，回车，把 bash 改成 Bash(*)，再回车保存。）